version: "3.9"

services:
  backend:
    build: ./msme-backend
    ports:
      - "5000:5000"
    env_file:
      - ./msme-backend/.env
    restart: always

  frontend:
    build:
      context: ./msme-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
